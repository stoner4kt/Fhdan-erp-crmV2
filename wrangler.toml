# Cloudflare Pages configuration.
# Keep Worker-only settings in workers/wrangler.toml; Pages validation rejects
# `main` and scheduled `triggers` in the root Pages config.
pages_build_output_dir = "."
